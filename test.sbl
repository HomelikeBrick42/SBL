proc plus(int int) -> (int) {
	+
}

proc do_something(int proc(int int) -> (int)) -> (int) {
	5 swap call
}

const value(3 3 +)

proc main() {
	value plus do_something call print

	1 while dup 10 <= {
		dup print
		1 plus call
	} drop
}

main call
