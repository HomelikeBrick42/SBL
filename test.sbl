proc add(int int) -> (int) {
	+
}

proc do_something(int proc(int int) -> (int)) -> (int) {
	5 swap call
}

const value(3 3 +)

proc main() {
	value add do_something call print

	1 while dup 10 <= {
		dup print
		1 add call
	} drop
}

main call
